#pragma once

#include <iostream>
#include <fstream>
#include <string>
#include "student.h"

// this libaray contains all the functions used in the teacher section
void addStudent(int _num)
{
	for (int i = 0; i < 3; i++)
	{
		cout << "Enter the ID: ";
		cin >> object[0].ID;
	}


}